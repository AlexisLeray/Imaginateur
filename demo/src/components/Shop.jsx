import React,{useContext} from "react"
 import {ReducerContext} from "./reducer/reducer.jsx"
import axios from 'axios'
import BASE_URL from "../config.js"

const Shop =() => {
    return(
        <h2>C'est la page Shop</h2>
        )
}
export default Shop